Operations
==========

These function are used by the Builder class to generate the shoe.

The objects manipulated are stored within ../Project.h. The classes
for the objects are within the ../object directory. 
